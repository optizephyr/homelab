# Placeholder — replace server_name and upstreams per host.
# Certbot: terminate TLS here or on the host and proxy to containers on homelab_edge.

server {
    listen 80;
    server_name _;

    location / {
        return 200 'homelab-stack nginx ok\n';
        add_header Content-Type text/plain;
    }

    # Example (uncomment when uptime-kuma profile is enabled):
    # location / {
    #     proxy_pass http://uptime-kuma:3001;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    # }
}
