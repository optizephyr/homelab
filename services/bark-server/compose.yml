services:
  bark-server:
    profiles: ["bark"]
    image: finab/bark-server:latest
    container_name: bark-server
    restart: unless-stopped
    volumes:
      - bark-data:/data
    networks:
      - homelab

volumes:
  bark-data:

networks:
  homelab:
    name: homelab
